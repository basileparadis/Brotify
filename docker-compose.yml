version: "3.2"
services:

  redis:
    image: "redis:latest"

    command: redis-server --requirepass pRoSiciTuSTA

    ports:
      - "6379:6379"

    environment:
     - REDIS_REPLICATION_MODE=master

  app:
    build: .
    environment:
      - REDIS_HOST=redis
    ports:
      - "80:8000"
